<% @id ||= 1 %>
<% current_product = t("collections.#{@collection}.p#{@id}") %>
<% n_products = t("collections.#{@collection}").keys.length %>

<h2 id="collection-name"><%= t("collections.index.#{@collection}").capitalize %></h2>

<h3 id="pic-title"><%= current_product[:title] %></h3>
<div id="more-details">
  <%= link_to t('collections.more_details'), collections_showpic_path(:name => @collection, :id => @id), {:id => 'more-details-link', :title => t('collections.more_details_title')} %>
</div>

<div id="collection">
  <% 1.upto(n_products) do |n| %>
    <% product = t("collections.#{@collection}.p#{n}") %>
  
    <%= link_to collections_showpic_path(:name => @collection, :id => n), {:title => product[:title]} do %>
      <%= image_tag "/images/collections/#{@collection}/pic/p#{n}.jpg", :alt => product[:description], :title => product[:title], 'data-price' => product[:price], 'data-etsy-link' => product[:etsy_link], 'data-link' => collections_showpic_path(:name => @collection, :id => n) %>
    <% end %>
  <% end %>
</div>

<% photographs = []; t("collections.#{@collection}").each{|name, product| photographs << product[:photograph]}; photographs = photographs.sort.uniq %>
<% models = []; t("collections.#{@collection}").each{|name, product| models << product[:model]}; models = models.sort.uniq %>

<div id="credits">
  <div><%= t((photographs.size > 1 ? 'photographs' : 'photograph'), {:name => photographs.join(', ')}) %></div>
  <div><%= t((models.size > 1 ? 'models' : 'model'), {:name => models.join(', ')}) %></div>
</div>

<script>
  Galleria.loadTheme('/javascripts/galleria/src/themes/arnela/galleria.classic.js');
  
  $(function () {
    
    $('#collection').galleria({
  
      show_imagenav: false,
      show_info: false,
      show_counter: false,
      clicknext: true,
      width: 750,
      transition: 'fade',
      thumb_quality: true,
      show: <%= (@id.to_i - 1).to_json %>,
      
      on_image: function(img,thumb) {
        var data = this.getData(this.getIndex());
        var original = $(data.original);
        // var price = original.data('price');
        // var etsyLink = original.data('etsy-link');

        $('#pic-title').text(data.title);
        //$('#pic-description .text').text(data.description);
        $('#more-details-link').attr('href', original.data('link'));
        //$('#etsy a').attr('href', etsyLink);
        //$('#price').text(price);
      },
      
      extend: function () {
        var that = this;
        
        //this.bindTooltip( this.get('images'), <%= raw t('collections.more').to_json %>);
        
        $(document).bind('keydown', function (e) {
          if (e.target.type == 'textarea' || e.target.type == 'input') { return true; }
  
          if (e.keyCode == 39) { that.next(); return false; }
          else if (e.keyCode == 37) { that.prev(); return false; }
        });
      }
    });
  });

</script>